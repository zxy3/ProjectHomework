/**=================================================================================================
 * @file	test2.31.cpp.
 *
 * Implements the test 2.31 class
 * 练习 2.31: 假设已有上一个练习中所做的那些声明， 则下面的哪些语句是合法的？ 请说明顶层 const 和底层 const 在每个例子中有何体现。
 * r1= v2;合法；
 * p1 = p2; 不合法，p1是非常量，p2是常量，不能进行赋值操作；     p2 = p1;合法；
 * p1 = p3; 不合法，p3既是底层也是顶层const，所以都得考虑到；    p2 = p3;合法
 *===============================================================================================**/
