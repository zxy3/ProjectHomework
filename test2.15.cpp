/**=================================================================================================
 * @file	test2.15.cpp.
 *
 * Implements the test 2.15 class
 * 练习 2.15: 下而的哪个定义是不合法的？ 为什么？
 * (a)int ival = 1.01;  非法 定义了double类型的值
 * (b) int &rvall = 1.01; 非法，引用只能跟对象绑定
 * (c)int &rval2 = ival; 合法（前提ival也是int类型，否则非法）
 * (d) int &rval3; 非法（定义时就要完成初始化，否则不知道是哪个对象的别名）
 *===============================================================================================**/
