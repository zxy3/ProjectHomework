/**=================================================================================================
 * @file	test5.7.cpp.
 *
 * Implements the test 5.7 class
 * 练习 5.7: 改正下列代码段中的错误。
 * (a)if (ival1!= ival2) 
 * ival2=ival1;
 * else ivall = ival2 = 0;
 * (b)if (ival < minval)
 * minval = ival;
 * occurs = 1;
 * (c)if (int ival = get_value())
 * cout << "ival=" << ival << endl;
 * if (lival)
 * cout « "ival = 0\n";
 * (d) if (ival = 0)
 * ival = get_value();
 *===============================================================================================**/
