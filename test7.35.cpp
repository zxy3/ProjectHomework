/**=================================================================================================
 * @file	test7.35.cpp.
 *
 * Implements the test 7.35 class
 * 练习 7.35： 解释下面代码的含义， 说明其中的 Type 和 initVal 分别使用了哪个定义。 如果代码存在错误， 尝试修改它。
 * typedef string Type;
 * Type initVal();
 * class Exercise {
 * public:
 * typedef double Type;
 * Type setVal(Type);
 * Type initVal();
 * private:
 * int val;
 * Type Exercise::setVal(Type parm) {
 * val = parm + initVal();
 * return val;
 *===============================================================================================**/
